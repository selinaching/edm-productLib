# 产品库需求文档 (PRD)

> 说明：本文初版为「需求表达模板」，用于回答"想 coding 之前该怎么表达需求"。
> 现在已按实际实现更新为**定稿版**，可直接作为产品库的说明书与后续迭代依据。
> 实现状态：✅ 已实现 / 🔶 待定

---

## 1. 项目背景

- **当前痛点**：负责多个品牌的海外邮件营销，每天写大量邮件；产品信息散落在各处，需要反复查找、逐条复制，效率低。
- **预期效果**：集中管理所有品牌的产品信息，写邮件时快速浏览 → 挑选 → 一次性复制所需文案。
- **使用场景**：写海外营销邮件时，打开产品库 → 选品牌 → 点击产品展开 → 勾选需要的功能/卖点/描述/CTA 加入右侧收藏篮 → 一键复制全部 → 粘贴进邮件。

---

## 2. 目标用户

- **使用者**：仅我一人，无需多人协作
- **技术水平**：不需要写代码，点按钮就能用
- **设备**：Mac，浏览器访问
- **产品形态**：Web 应用（浏览器打开即用）

---

## 3. 核心数据结构

### 3.1 实体关系

```
品牌 (Brand) 1:N ── 产品 (Product) 1:N ── 内容值 (Value)
                                          └── Value = { 标签, 内容 }

列定义 (Field) 1:N ── 内容值      ← 列可自由增删改名，见 3.5
```

### 3.2 品牌 (Brand) ✅

| 字段 | 类型 | 说明 |
|------|------|------|
| 品牌名称 | 文本 | 必填 |
| 备注 | 长文本 | 可选（如"电子产品品牌"） |

### 3.3 产品 (Product) ✅

| 字段 | 类型 | 说明 |
|------|------|------|
| `name`（界面显示：**SKU/产品名**） | 文本 | 必填。可填 SKU 编码，也可填产品名，或两者一起 |
| 所属品牌 | 关联品牌 | 必填 |

### 3.4 内容值 (Value) ✅

每个产品在每个**列**下可以有任意多条内容，每条内容由两部分组成：

| 组成 | 说明 | 示例 |
|------|------|------|
| 标签 | 可选，用于备注这条内容的重点 | `短描述`、`长描述`、`核心参数` |
| 内容 | 正文文案 | `主动降噪 ANC` |

- 标签在界面上显示为蓝色小标签，在复制结果中显示为 `[标签]内容`
- **描述**列尤其依赖标签，用于区分"短描述/长描述"等不同版本文案
- 其他列（功能、卖点、CTA）标签可选，通常留空

### 3.5 列定义 (Field) ✅ —— 表头可自由配置

> 这是定稿版相较初版最大的变化：**内容列不再是固定 4 个，而是可自由增删改名。**

- 默认 4 列：`参数/功能`、`卖点`、`描述`、`引导购物语`
- 每一列可：**重命名**、**新增**、**删除**、**上下调整顺序**
- 列配置是**全局的**，所有品牌共用同一套列（如需按品牌区分，可后续扩展）
- 删除列会同时删除所有产品在该列下的内容（有二次确认提示）

| 默认列名 | 说明 | 示例值 |
|----------|------|--------|
| 参数/功能 | 技术参数或功能点 | `防水 IP68`、`续航 36 小时` |
| 卖点 | 营销卖点 | `沉浸式降噪体验` |
| 描述 | 产品描述文案（常用标签区分长短版本） | `[短描述]采用新一代 13mm 动圈单元…` |
| 引导购物语 | CTA 文案 | `限时 8 折，立即抢购` |
| （可新增） | 如"适用人群""尺寸规格"等 | 自定义 |

### 3.6 按钮库 (Button Library) ✅ —— 独立于产品的 CTA 文案池

不同场景下反复用到的按钮文案（CTA）单独维护成一个全局库，避免每次手打。

| 字段 | 说明 | 示例 |
|------|------|------|
| 场景 | 文案适用场景，用于分组 | `限时折扣`、`新品上市`、`会员专享`、`免费试用`、`通用` |
| 按钮文案 | 按钮上的文字 | `Shop Now — Save 20% Today`、`Add to Cart` |

- 库的条目**不属于任何产品**，是全局共享的池子
- 产品需要时从库中挑选，挑中后成为该产品对应列下的一条内容（场景自动作为标签）
- 因此改库不影响已加入产品的内容；反之亦然
- 支持批量导入（CSV/Excel，两列：场景、按钮文案）

### 3.7 价格列约定 ✅ —— 自动识别与优惠计算

> 设计选择：**不新增字段类型**，而是复用已有的「标签」机制。
> 这样用户无需学习新概念（和"短描述/长描述"用法一致），数据模型也不用改。

**输入方式**：在价格列填两条内容，用标签区分

| 标签 | 内容示例 | 含义 |
|------|---------|------|
| `现在价格` | `49.99` | 当前售价 |
| `划线价` | `$79.99` | 原价 / 划线价 |

- 金额支持 `$49.99`、`1,299.00`、`79` 等写法（自动抽取数字）
- **识别词**（大小写不敏感）：
  - 现价：`现在价格`、`现价`、`售价`、`current`、`now`、`sale`
  - 原价：`划线价`、`原价`、`original`、`list`、`was`、`reg`、`msrp`
- 价格列的添加框会自动把标签提示改为「现在价格 / 划线价」

**自动计算**：优惠金额 = 划线价 − 现在价格

**三种可选文案**（卡片上点按钮即加入待复制内容）

| 形式 | 示例 | 说明 |
|------|------|------|
| `SAVE $xx` | `SAVE $30` | 强调"省了多少" |
| `$xx OFF` | `$30 OFF` | 强调"减了多少" |
| `xx% OFF` | `38% OFF` | 强调"折扣力度" |

**格式化与边界规则**

| 情况 | 处理 |
|------|------|
| 优惠为整数 | `$30`（不带小数） |
| 优惠带零头 | `$30.50`（补两位，符合货币书写） |
| 现价 ≥ 划线价 | 不显示优惠，避免出现负数或虚假折扣 |
| 折扣比例四舍五入为 0（如 100→99.9） | 不提供 `% OFF` 形式，避免出现 `0% OFF` |

---

### 3.8 导入分条规则 ✅ —— 只认「短横线 -」

> 用户定稿：一格内要写多条内容时，**只用短横线 `-` 分条**，其他任何符号都不做分条识别。
> 规则越少越可预测——写什么就是什么，系统不猜。

**唯一的组合方式**

| 用途 | 符号 | 示例 |
|------|------|------|
| **分条**（一条 → 多条） | 短横线 `-`（行首，或前面带空格） | `主动降噪 ANC - 续航 36 小时 - 蓝牙 5.3` |
| **标签**（给单条加备注） | `标签\|内容`，也支持 `标签：内容` / `标签:内容` | `短描述\|采用 13mm 动圈单元` |

两者可以组合：`短描述|13mm 动圈 - 长描述|蓝牙 5.3` → 2 条，各自带标签。

**🚫 明确不做分条的符号**（全部当作内容的一部分原样保留）

| 符号 | 例子 | 结果 |
|------|------|------|
| 分号 `;` `；` | `实木底座;暖光 2700K` | **1 条**，分号留在内容里 |
| 项目符号 `• · ▪` | `• 降噪 • 续航` | **1 条** |
| 序号 `1. 2. 3.` / `1)` / `1、` | `1. 降噪 2. 续航` | **1 条** |
| 句号 / 问号 / 感叹号 | `经典设计 灵感源自…。高级材质 选用…。` | **1 条** |
| 长段里的「短标题 + 说明」 | `Classic Design. Inspired by…` | **1 条**，不再自动切成标题+内容 |
| **单元格内换行（Alt+Enter）** | 一句讲材质、一句讲场景 | **1 条**，换行原样保留（界面和复制结果都是两行） |

**连字符不会误切**

- 只有「行首」或「前面是空格」的 `-` 才算分条符
- `mid-century`、`5-10 分钟`、`2024-2025` 里的 `-` 前后没有空格，**不切**
- 反过来 `A - B` 一定切 —— 所以内容里不要随便用 `空格-空格`

**其他规则**

- 导入/导出用同一个分隔符：导出的 Excel、CSV 里多条内容用 ` - ` 连接，可原样再导入回去
- 导入模板的示例也改成了 ` - `
- 已经导入成一大坨的内容：详情里长内容旁边有 **`分点`** 按钮，点一下原地拆开；
  列头的 **`一键分点`** 把该列所有能拆的一次性拆开（同样只认 `-`）
- 行内编辑时 `Enter` 保存、`Shift+Enter` 换行；内容区 `white-space: pre-line`，换行显示为两行

**重要：描述列的标签只给自己看**

- 沿用既有规则：描述列的标签（`短描述`/`长描述`/`Classic Design` 等）**不进入**待复制内容
- 想让标签也带进邮件 → 删除该条标签，或告诉我把这条规则反过来

---

## 4. 关键设计决策

### 4.1 多值字段的展示方式 ✅ 已定稿

> 核心难题：每个产品有 N 个功能、N 个卖点，且 N 不固定。

曾评估过固定列（方案A）、展开行（方案B）、合并单元格（方案C）、主表+详情面板（方案D）。

**最终方案：主表 + 行内展开 + 右侧收藏篮**

- 主表一行一个产品，显示产品名、品牌，以及每一列的内容条数（如"功能 6"）
- **点击产品行，详情在该行下方就地展开**（在主区表格内，不是右侧面板）
- 展开后按列分区显示所有内容，每条内容可单独：加入收藏篮 / 复制 / 编辑 / 删除
- 每列区块提供"全部加入""全部复制"
- 再次点击该行收起

理由：主表保持干净可快速浏览；详情在主区内展开，视线不用来回跳跃；配合收藏篮可跨产品、跨列累积挑选。

### 4.2 收藏篮与复制格式 ✅ 已定稿

写邮件时通常需要**跨多个列**挑选内容，逐条复制太慢，因此引入收藏篮：

- 右侧常驻面板「待复制内容」，点击内容的「加入」即累积
- 面板**按列分组展示**，与最终复制结果完全一致（所见即所得）
- 底部「全部复制 (N)」一键复制全部，「清空」重置
- 收藏篮内容会持久化保存，刷新不丢

**复制输出格式（重点）：**

```
[参数/功能]
主动降噪 ANC
续航 36 小时

[卖点]
沉浸式降噪体验

[描述]
[短描述]采用新一代 13mm 动圈单元…
[长描述]搭载最新蓝牙 5.3 芯片…
```

规则：
1. **同一列只出现一次 `[列名]` 标题**（不是每条都带前缀）
2. 组内每条**单换行**
3. 组与组之间**恰好一个空行**
4. 有标签的内容显示为 `[标签]内容`

### 4.3 按钮库的使用方式 ✅ 已定稿

- 按钮库是**全局池**，与产品解耦；产品的每一列都可以从中挑选（不只是按钮列）
- 入口：顶部「按钮库」管理库；产品展开后每列区块的「库选」按钮进行挑选
- 挑选弹窗支持按场景分组浏览 + 关键词搜索，可连续挑选多条
- 挑中后加入该产品该列，**场景自动作为标签**（可关闭"带入场景标签"开关）
- 带场景标签时复制输出为 `[限时折扣]Shop Now — Save 20% Today`

### 4.4 搜索与筛选 ✅

- 按品牌筛选：是（左侧品牌列表，含"全部品牌"）
- 关键词搜索：是（匹配产品名，以及所有列的内容）
- 搜索与品牌筛选可叠加

---

## 5. 功能清单

| 优先级 | 功能 | 状态 | 说明 |
|--------|------|------|------|
| P0 | 品牌管理 | ✅ | 新增/编辑/删除品牌 |
| P0 | 产品管理 | ✅ | 新增/编辑/删除产品，指定所属品牌 |
| P0 | 内容管理 | ✅ | 每列下无限添加内容，支持标签、行内编辑、单条删除 |
| P0 | 行内展开详情 | ✅ | 点击产品行在主区展开，按列分区 |
| P0 | 收藏篮 + 一键复制 | ✅ | 跨列累积挑选，按分组格式一次复制 |
| P0 | **列管理** | ✅ | 表头列重命名 / 新增 / 删除 / 排序 |
| P0 | **按钮库** | ✅ | 按场景管理 CTA 文案，支持单条添加与批量导入 |
| P0 | 从库挑选 | ✅ | 任意产品任意列点「库选」挑用，场景自动作标签 |
| P0 | 导入模板 | ✅ | 下载跟随当前列结构的 Excel 模板（含按钮库 sheet） |
| P0 | **价格优惠计算** | ✅ | 识别现价/划线价，生成 SAVE $xx / $xx OFF / xx% OFF 三种文案供选 |
| P0 | 表格视图 | ✅ | 按品牌筛选，显示每列内容条数 |
| P1 | 搜索 | ✅ | 产品名 + 全列内容全文搜索 |
| P1 | 导入 | ✅ | CSV / Excel / JSON 备份；一格多条只认 ` - ` 分条（3.8 节） |
| P1 | 导出 | ✅ | CSV / Excel / 邮件文案 txt / JSON 备份 |
| P2 | 排序 | 🔶 | 按产品名/创建时间排序 |
| P2 | 多语言 | 🔶 | 中英文界面切换 |
| P2 | 品牌独立列配置 | 🔶 | 每个品牌使用不同列 |
| P2 | 版本历史 | 🔶 | 误删恢复 |

---

## 6. 界面需求

- **布局**：三栏 —— 左侧品牌列表 | 中间产品表格 | 右侧收藏篮（360px）
- **表格**：表头固定，内容区滚动；列数随列配置动态变化
- **展开**：点击产品行，详情在该行下方展开，背景色区分
- **编辑**：内容双击或点「编辑」进入行内编辑（标签框 + 内容框），Enter 保存、Esc 取消；Enter 换行用 Shift+Enter
- **添加**：每列底部有「+ 添加XX」按钮，弹出标签框 + 内容框，Enter 连续添加
- **响应式**：窗口 < 1024px 时收藏篮变为侧滑抽屉

---

## 7. 技术方案

| 维度 | 决定 | 说明 |
|------|------|------|
| 形态 | **Web 应用** | 浏览器打开即用 |
| 前端 | 纯 HTML + CSS + JS | 无构建工具，无框架 |
| 数据存储 | **localStorage** | 单人使用，存本地浏览器，无需后端 |
| 存储键 | `productLibrary_v3` | 自动兼容迁移 v1 / v2 旧数据 |
| 导入/导出 | SheetJS (xlsx) | CDN 加载，支持 CSV / Excel |
| 部署 | 本地运行 | 双击 HTML 或本地起静态服务 |
| 可选云同步 | GitHub API（contents / commits 端点） | 把整份数据当 JSON 推到私有仓库；每次推送是一次 git 提交，天然带版本历史；支持跨设备恢复；自动同步模式（改动后 30 秒内最多推一次） |

### 数据模型（v3）✅

```js
state = {
  fields:   [ { id, name }, ... ],        // 动态列定义
  brands:   [ { id, name, note }, ... ],
  products: [ { id, brandId, name,
                values: { 列id: [ {label, content}, ... ] } } ],
  basket:   [ { fieldId, label, content }, ... ],  // 收藏篮
  buttonLibrary: [ { id, scene, content }, ... ],  // 按钮库（全局 CTA 池）
}
```

- 列 id 与内容通过 `values[列id]` 关联，因此**重命名列不会丢失数据**
- 导入/导出的列顺序跟随当前列配置
- 旧版本数据（固定 `features/sellingPoints/descriptions/ctas` 字段）会在加载时自动迁移

---

## 8. 需求确认汇总

**初始确认：**

1. **数据量**：7-10 个品牌，每品牌产品数不固定（几个到几十个），功能/卖点数多且不固定 → 需无限添加
2. **多人协作**：不需要，单人使用
3. **跨设备同步**：有更好，没有也行 → 通过 JSON 备份/恢复实现
4. **导入已有数据**：需要（CSV / Excel）
5. **导出功能**：需要（CSV / Excel + 邮件模板）
6. **数据备份**：需要（JSON 导出/导入，防止 localStorage 丢失）

**后续追加确认：**

7. **详情展示位置**：点击产品后信息**在主区内展开**，不是右侧面板
8. **批量复制**：需要一个"格子"累积所选内容，最后一次性复制
9. **描述标签**：每条描述前可加备注标签，格式为 `[标签][描述]`
10. **复制去重**：同一列只保留一个列标题，只保留一个空行
11. **表头可配置**：列名可编辑修改，列可新增、删减
12. **按钮库**：需要一个独立于产品的 CTA 文案库（按场景分类），可从库中挑用；每个产品都能这么操作
13. **导入模板**：需要一份可填写的案例模板（产品库 + 按钮库），且表头跟随当前自定义列
14. **价格优惠**：价格列需自动识别"现在价格"与"划线价"，算出优惠，并提供 `SAVE $xx` / `$xx OFF` / `xx% OFF` 三种形式供选择插入

---

## 9. 使用提示

- **首次使用**：内置了 3 个品牌、4 个产品的示例数据，可直接体验；正式使用时删除即可
- **日常流程**：选品牌 → 点开产品 → 挑内容「加入」→ 右侧「全部复制」→ 粘贴进邮件
- **用按钮库**：顶部「按钮库」先按场景备好常用 CTA → 写邮件时在产品对应列点「库选」→ 挑一条直接加入
- **备份习惯**：定期用「导出 → 备份数据 (JSON)」保存，避免清理浏览器数据时丢失
- **批量录入**：用「导出 → 下载导入模板」拿到跟随你当前列结构的 Excel，按格式填好再导入
- **云同步（推荐）**：顶部「云同步」→ 填一个 GitHub 私有仓库 + 令牌 → 点「保存并上传」；之后勾上「改动后自动上传」就能自动备份，换电脑点「从 GitHub 拉取」一键恢复。每次推送是 GitHub 的一次 commit，有完整版本历史。

### 导入格式说明

| 规则 | 说明 | 示例 |
|------|------|------|
| 前两列固定 | 品牌名称、SKU/产品名（产品不存在会自动创建） | `TechPro` / `HS-AURORA-01` |
| 后续列 | 与你当前的自定义列一一对应（按列的左右顺序） | 参数/功能、卖点、描述、按钮… |
| **分条（唯一）** | 同一列要写多条 → 用短横线 ` - ` 分隔；行首的 `- ` 也算 | `蓝牙 5.3 连接 - 主动降噪 ANC - 续航 36 小时` |
| 标签前缀 | 内容前加 `标签\|`（也支持 `标签：`、`标签:`）即为该条打标签 | `短描述\|采用新一代 13mm 动圈单元` |
| **不再识别** | 分号、项目符号、序号、句号、`Classic Design. xxx.` 这类短标题配对 —— 全部当作内容，不拆 | 见 3.8 节 |
| **换行不是分隔符** | 单元格内换行是内容本身（要分两行），整段保留为一条 | `材质说明。\n场景说明。` → 1 条两行 |
| 连字符安全 | `mid-century`、`5-10 分钟` 前后没有空格，**不会**被切成两条 | — |
| 去重 | 导入时内容完全相同的条目会自动跳过，不会重复添加 | — |
| 按钮库 | 两列：场景、按钮文案；可与产品库放在同一文件的不同 sheet | `限时折扣` / `Shop Now` |
